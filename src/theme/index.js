import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#003BE5',
    },
    secondary: {
      main: '#2FC18C',
    },
    inherit: {
      main: '#FFFFFF',
    },
  },
});

export default theme;
